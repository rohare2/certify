# $Id: spec 509 2014-05-22 05:47:07Z rohare $
# $URL: file:///usr/local/svn/certify/trunk/nfs/sun/spec $
#
# Author: Rich O'Hare  <rohare2@yahoo.com>
#
# System security certification scripts
#
%define Name certify-nfs-sun
%define Version (version)
%define Release (release)
%define Source (source)

Name: %{Name}
Version: %{Version}
Release: %{Release}
Source: %{Source}
License: GPLv2
Group: Applications/System
BuildArch: noarch
URL: https://www.ohares.us/
Distribution: RedHat/Centos 6
Vendor: Rich O'Hare
Packager: Rich O'Hare <rohare2@gmail.com>
Provides: sun_diskscan.sh sscs_report.sh
Requires: mysql >= 5.0
Summary: Certify disk scanning tool for SUN storage units
%define _unpackaged_files_terminate_build 0

%description
Disk scanning tool that works with Certify application to detect and
track disk drives on SUN storage appliances. SUN sscs command used
to detect and report on the disk drives. A disk inventory report is
sent to /var/log/messages.

Peristent data is stored in a mysql database available from certify-db rpm.

%prep
%setup -q -n %{Name}

%build
exit 0

%install
#rm -rf %RPM_BUILD_ROOT/*
make install
exit 0

%clean
#rm -fR %RPM_BUILD_ROOT/*
exit 0

%files
%defattr(-,root, root)
%config %attr(764, root, wheel) /etc/cron.daily/sun_diskscan.cron
%attr(740, root, root) /usr/local/sbin/sun_diskscan.sh
%attr(644, root, root) /usr/share/doc/%{Name}-%{Version}/readme
%attr(644, root, root) /usr/share/doc/%{Name}-%{Version}/sscs_report.sh
